## Installation

```sh
git clone --recurse-submodules git@github.com:naitian/nvim.git
```

You will also need `tree-sitter-cli` for treesitter highlighting.

Optionally, you may want to install `ripgrep`.
